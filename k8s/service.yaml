apiVersion: v1
kind: Service
metadata:
  name: llmmllab-runner
  namespace: llmmllab
  labels:
    app: llmmllab-runner
spec:
  type: ClusterIP
  selector:
    app: llmmllab-runner
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  publishNotReadyAddresses: true
